# AI-Coding-Playbook

## 项目定位

本库押「用好 AI」而非「开发 AI」：多数研发接不到 agent 开发的活，但把 AI 用好今天就见效，收益确定性更强。所有内容取舍服从这个判断。（完整论述见 README）

一套有结构、有框架的人机协作指南：沿「用 AI 完成一个开发任务」的链路分层，每层讲清这个环节怎么跟 AI 配合、会撞上什么问题、怎么解决。主线工具 Claude Code。

### 本库不是什么（立题时用它排除）

- 不是 AI agent 开发教程
- 不是 prompt 技巧合集
- 不是工具功能说明书
- 不是经验贴堆叠 —— 落不进能力栈说明立题有问题

### 成功标准

读完能与 AI 配合得更好、交付得更好；撞上问题 30 秒定位到对应文章并照做。

## 七条内容准则（写作与审校的最高标准）

前 3 条管「选题和解法」，后 4 条管「可信度」。

1. **问题要真、且有人关心**：必须是开发者实际会撞上的高频问题，要有真实触发场景和症状，不接受从文档/概念推演出的伪问题。立题前先过一关："一个天天用 AI coding 的开发者，会不会带着这个问题来搜？"已有共识的问题（如订阅比 API 划算）、只能停在理论层、给不出行动的推演题（如「AI 生成物到底有没有著作权」的法理之争）不立题；同一题材里可操作的部分（许可证扫描、数据出境通道），并入读者会去搜的那一篇（如 072 安全红线），不单独立题。
2. **解法可操作**：读者看完能"下次遇到直接照做"。要给步骤、可复制的命令/配置、以及"怎么判断做对了"的验证方法，不能只讲原则。
3. **分类服务检索**：读者带着问题来查，不从头读到尾。标题用"遇到问题的人会搜的话"表述，开头 30 秒给结论，小标题支撑跳读。
4. **判断标准必须可观测**：凡是让读者做判断的地方（该不该压缩、要不要停、算不算通过），不许写"感觉""觉得""好像"。给能看见的信号 —— 具体现象、具体输出、具体数字。写不出可观测信号，说明这个判断你自己也没想清楚。
5. **一篇之内数字必须自洽**：立论用的数字和行动用的数字不能打架（反例：一边说"质量 50% 开始下滑"，一边说"到 60% 再压"）。写完把全篇的数字放一起对一遍。
6. **数字断言要标硬度**：官方文档明写的、社区实测未确证的、个人经验的，三者在文中要能区分。查不到官方出处就明说"未见官方确证"，不要含糊带过。
7. **观点也要标硬度**：每篇的解法明示属于哪一档——「有定论」（直接给答案）、「多解看场景」（给决策树，mermaid 图就是结论）、「未定论」（摆各派主张 + 适用边界 + 本库当前倾向，明写"未定论"）。不许把一家之言写成共识。

## 目录结构

- `01`~`09` 九个主题目录，按「用 AI 完成项目的能力栈」自下而上分层（见 README 图）；其中 `07 效率与成本`、`09 工具可靠性与供应商风险` 是横切层，不占栈内位置
- 各类编号起点：01→001、02→010、03→021、04→030、05→040、06→050、07→060、08→071、09→080；类内编号连续，新增文章接在本类最大编号之后
- 命名统一用中文：目录 `NN-中文主题`，文件 `NNN-中文简述.md`；文件名不带标点符号（`？、，：` 一律去掉），英文专名保留原形并用连字符连接（如 `004-Claude-Code能力地图.md`）
- 文章 H1 格式 `# NNN. 标题`，编号必须与文件名一致
- `_templates/qa-template.md`：单篇模板
- `scripts/validate-qa.sh`：结构校验脚本

## 单篇结构（5 段）

```
# NNN. 标题
**一句话**：……

## 30 秒结论      速查表 +（决策/流程类）一张 mermaid 图
## 怎么做         步骤 + 命令 + 「怎么确认生效」；深水区折进 <details>
## 为什么         原理，3 小节以内
## 别这么干       ≤ 5 条
## 延伸           相关文章 + 参考资料 + 脚注 + 文末元信息
```

排版是本库的第一道体验关，读不下去等于没写。硬性要求：

- **正文开头只有标题和一句话结论。** 难度、题型、工具、时效声明全部下沉到文末 `<sub>` 小字。读者第一眼必须看到答案，不是元数据。
- **默认可见的正文控制在 120 行内，硬上限 140 行。** 口径以 `validate-qa.sh` 为准：全文件范围内、`<details>` 折叠块之外的非空行（空行和折叠块内容都不算）。120 是目标值，超了先想能不能折；140 是脚本告警的红线。压不下去时优先保 30 秒结论表、mermaid 图和核心命令，别为凑行数删掉可操作的部分。配置细节、环境变量表、横向对比、易变清单塞进 `<details>` 折叠块。`<summary>` 后面必须空一行，否则 GitHub 不渲染块内 Markdown。
- **`怎么做` 排在 `为什么` 前面。** 读者带着问题来，先给动作。原理挡在动作前是「读不下去」的头号原因。
- **配图过三关才画，过不了就用表格或文字。** ①**结构关**：论点本身是分支、顺序、循环、层次或状态迁移，用文字说读者得在脑内组装零件；②**决策关**：读者能不能只看这张图就在 30 秒内定下一步动作；③**论点关**：能不能用一句话写出图注。三关缺一就别画 —— 目标不是「每篇都有图」，是「每个值得画的结构有唯一一张好图」。三个以上分支的选择题不要用文字描述。
- **每张图下面必须有一句话图注，写这张图的结论，不是复述流程。** 反例「上图是上下文压缩的判断流程」（复述）；正例「换任务就先 `/rename` 再 `/clear`，别在同一会话里硬接下一个任务」（结论）。写不出一句话图注，说明这张图没有独立存在的理由，删掉。
- **默认一篇一张图**，放在 `30 秒结论` 段；节点 ≤ 12 个，标签 ≤ 两行、单行 ≤ 12 个中文字，用 `<br/>` 折行。只有当另有一个**完全独立的判断维度**（不是第一张图的细化展开、不是原理说明）自己也能过三关、且并进第一张会超 12 节点时，才允许加第二张，放在 `怎么做` 段对应小节。想加第三张，说明这篇装了两个问题，拆篇，别加图。
- **配图负面清单**：不把并列要点画成一排盒子（那叫列表）；不画 20+ 节点全景图（那是地图不是结论）；不在节点里塞长句；不用 `classDef` 上色（本库都是 12 节点以内的小图，收益低还吃 120 行正文预算）；不放 PNG / SVG / drawio 资产文件 —— 一律 Mermaid 内嵌，图片资产无法 diff，双周扫描改了文字不会同步改图，几轮后必然图文漂移。
- **默认只用 `flowchart`。** 交互与调用顺序才用 `sequenceDiagram`，状态迁移才用 `stateDiagram-v2`；只用 GitHub 能渲染的基础语法，实验性特性不进库。
- **原「Claude Code 实战」并入 `怎么做`**，原「横向对比」折叠进 `别这么干` 之后。主线工具就是 Claude Code，两段分开必然重复。
- 新增或改结构后必须跑 `scripts/validate-qa.sh` 校验

## 写作规范

- 中文，直白文风：不自造词、不用文学化比喻、不堆砌术语；必须用术语时一句话解释
- **正文里不出现行内来源标注**：不写括号来源、不写 `✅` 标记、不贴英文原文。关键论断用 `[^n]` 脚注，其余出处统一进文末「参考资料」。可信度不靠在每句话后面挂链接来证明。
- 涉及价格、模型版本、工具行为等时效性内容，在文末「时效」小字里说明核实日期与易变项
- 外链参考资料标注来源类型（官方 / 社区 / 实践）与日期
- Anthropic 官方站有两类地址，别混：**文档**已迁到 `code.claude.com/docs/en/*`（旧的 `anthropic.com/engineering/claude-code-*` 会 308 跳过去），**工程博客**仍在 `anthropic.com/engineering/*` 且正常返回 200。批量替换前先 `curl -o /dev/null -w "%{http_code}"` 逐个确认，别把有效博客链接改坏
- 交叉引用锚文本必须与目标文章标题主题一致
- 正文里用编号指代其他文章（如「见 021 篇」）前，先确认该篇真实存在；合并或废弃文章时必须回头清掉指向它的编号引用
- 每篇末尾保留 `本篇个人实践（L4）` 占位，由 BOSS 补实战经验

## 本地预览

```bash
scripts/render.sh 02-上下文工程/012-上下文窗口要爆了.md   # 单篇
scripts/render.sh --all                                    # 全库
```

首次运行自动建 `.venv` 装依赖。产出的 `.html` 已被 gitignore —— 它只是本地预览和分享用，GitHub 上读者看的是 `.md` 原文（mermaid、`<details>`、表格、脚注 GitHub 都原生渲染）。

## README 维护约定

README 结构顺序：判断 → 框架 → 两种用法 → 完整目录 → 元信息。先立框架再给查询入口，顺序反了就退化成故障手册，丢掉结构性这个核心价值。

- 首屏不滚动就能回答「这是什么、为什么值得看」
- 长内容用 `<details>` 折叠
- mermaid 节点 ≤ 两行、单行 ≤ 12 个中文字（超了会在窄容器里撑破边框）
- 「完整目录」段与 `llms.txt` 是 `scripts/gen-toc.sh` 的生成产物，不许手改；`validate-qa.sh` 会用 `--check` 拦截手改和漂移
- 篇数、结构描述必须与实际文件一致

## 修改约定

- 新增/改名文章后跑 `scripts/gen-toc.sh` 重新生成 README 目录段与 `llms.txt`
- 改文件名必须同步改交叉引用：跑一遍内部链接校验，确认 0 条失效再提交
- 文末「时效」核实日期超 6 个月会被 `validate-qa.sh` 警告，复核后更新日期
- 生成的 HTML 是可再生产物，不进仓库（`.gitignore` 已忽略）
